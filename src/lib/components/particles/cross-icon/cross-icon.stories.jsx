import { CrossIcon } from '.'

const meta = {
  title: 'Particles/CrossIcon',
  component: CrossIcon,
  decorators: [
    (Story) => (
      <div style={{ width: 17, height: 17 }}>
        <Story />
      </div>
    ),
  ],
}

export default meta

export const Default = {}
